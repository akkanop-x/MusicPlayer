# 06 LikeButton + SearchPage + AddToPlaylistDialog + LIKES_CHANGED handler

Status: resolved

- heart ใน search rows / playlist rows / liked / history
- realtime handlers.ts: LIKES_CHANGED → update cache
  Evidence: —
  Lessons: —
