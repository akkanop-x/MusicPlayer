# 04 — GET /api/v1/stream/:trackId + cookie auth + rate limit

Status: resolved

> _This was generated by AI during triage._

## Context

api.md §6 (stream endpoint — auth พิเศษด้วย session cookie) + §12 (60/min/user + ≤2 concurrent/user) + security.md §8.6

## Work

1. `routes/stream.routes.ts`: GET `/api/v1/stream/:trackId`
   - auth: verify `refresh_token` cookie (ตัวเดียวกับ /auth/refresh — ต้องยัง valid ไม่ revoked) → ไม่ผ่าน 401 UNAUTHENTICATED
   - trackId ต้องเป็น UUID → ไม่ใช่ 400 VALIDATION_ERROR; ไม่พบใน DB → 404 NOT_FOUND
   - เรียก StreamService.openStream → ส่ง bytes กลับ (pipe, ไม่ buffer ทั้งไฟล์)
   - rate limit in-memory: 60 req/min/user; concurrent ≤ 2/user (เกิน → 429 RATE_LIMITED / 409?)
2. Cleanup: ปิด upstream เมื่อ client abort/ปิด connection

## Acceptance

- 401 โดยไม่มี/cookie หมดอายุ; 429 เมื่อเกิน 60/min; stream ที่ 3 พร้อมกัน → 429
- ไม่มี response ใด (ทุก endpoint) คืน stream URL จริง

## Comments

## Comments

> _This was generated by AI during triage._

**resolved (2026-09-20):** route auth ด้วย refresh cookie แบบ read-only (`AuthService.validateSession` — ไม่ rotate); rate limit 60/min/user + concurrent ≤ 2 (in-memory, release เมื่อ connection ปิด); trackId validate UUID → 400; hijack raw response + pipe จริง. ผลจริง: ไม่มี cookie → 401; cookie ถูก → 206 audio/webm 3.4 MB; ไม่มี response ใดคืน stream URL
