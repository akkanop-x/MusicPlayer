# 08 — ถอด Spotify ออกทั้งสแตก → YouTube ล้วน (ADR-009)

Status: resolved

> _This was generated by AI during triage._

## Context

สปอนซ์: Spotify Web API คืน 403 "Active premium subscription required for the owner
of the app" ให้ ticket 05 — genre enrichment และ `spsearch` ใช้ไม่ได้จริง ผู้ใช้ตัดสินใจ
2026-09-20: **เปลี่ยนจาก Spotify เป็น YouTube ให้หมด รวมถึงการ search —
เอา metadata ตามที่ YouTube ให้มาแบบดิบ** → [ADR-009](../../docs/adr/009-drop-spotify-youtube-only.md)

## Work

1. SearchService: ถอด source `sp` ออกจาก `SEARCH_SOURCES` (เหลือ `yt` default + `ytm`);
   `source=sp` → 400 VALIDATION_ERROR; ถอด deps `enrichGenres`/`enrichBudgetPerRequest`
2. ลบ `SpotifyWebApiService.ts` + wiring ใน app.ts; ถอด `SPOTIFY_CLIENT_ID/SECRET`
   ออกจาก env schema / compose (lavalink + server) / `.env.example` / `.env`
3. application.yml: ถอด LavaSrc plugin + `lavasrc:` config — เหลือ youtube-source 1.18.2
4. Genre enrichment ใหม่จาก YouTube เอง: resolver อ่าน yt-dlp `categories`+`tags`
   (trim/lowercase/dedupe, จำกัด 15) → คืน `genres` ใน `/resolve` → StreamService
   persist ผ่าน `updateGenres` แบบ fire-and-forget ตอน stream ถูกเปิด
5. smoke-lavalink: step 3 เปลี่ยน `spsearch:` → `ytmsearch:`
6. Docs: ADR-009 (ใหม่) + database/lavalink/architecture/README/audio-pipeline/
   requirements/recommendation/roadmap + หมายเหตุ supersede ใน ADR-008

## Acceptance

- Search API ตอบเฉพาะ source YouTube (`yt`/`ytm`), `sp` → 400
- Stream เพลง → `tracks.genres` ถูกเติมด้วย tags ดิบของ YouTube
- ไม่มี SPOTIFY env เหลือใน container ใด; Lavalink โหลดแค่ youtube-plugin

## Evidence (2026-09-20)

- `bun apps/server/scripts/smoke-lavalink.ts` → SMOKE PASS: sourceManagers
  `soundcloud, bandcamp, youtube, http`, plugins `youtube-plugin@1.18.2`,
  `ytsearch` 20 tracks + `ytmsearch` 20 tracks
- typecheck/lint/format ผ่านทั้ง monorepo; tests **83/83** เขียว
- compose rebuild แล้ว: `docker exec` ทั้ง lavalink/server ไม่มี SPOTIFY env (grep exit 1)
- `GET /api/v1/search?q=...` → TrackDTO sourceName `youtube`, `source=sp` → **400**
- `GET /api/v1/stream/c85a2d77-...` → **200 audio/webm**; หลัง stream:
  `tracks.genres = {music,"rick astley","never gonna give you up",nggyu,...}` (14 ค่า —
  category `music` + raw tags ดิบของ YouTube ตามที่ขึ้นบนหน้าเว็บ แบบไม่ map)
- resolver `/resolve` คืน keys `[contentType, durationMs, genres, streamUrl]`
