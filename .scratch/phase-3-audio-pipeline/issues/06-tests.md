# 06 — Tests: SSRF suite + Range + cookie auth + resolver-down

Status: resolved

> _This was generated by AI during triage._

## Work

ทั้งหมด hermetic (mock HTTP server / mock resolver, ไม่แตะ docker/youtube):

1. SSRF suite: URL นอก allowlist → ปฏิเสธ; hostname แก้ได้ IP private/loopback/link-local/metadata → บล็อก; redirect ไป private → บล็อก; >3 redirects → บล็อก; content-type ไม่ใช่ audio/* → บล็อก
2. Range correctness (mock upstream): `Range: bytes=N-` → 206 + Content-Range/Content-Length passthrough; ไม่มี Range → 200 + Accept-Ranges
3. Prebuffer: upstream ส่งช้า → response ไม่ออกก่อนครบ prebuffer; upstream stall → abort ใน 30 s (ทดสอบด้วยเวลาย่อ)
4. Cookie auth: ไม่มี cookie → 401; cookie invalid/revoked → 401; ใช้ refresh cookie ที่ valid → ผ่าน
5. Resolver-down: circuit breaker/timeout → 503 ภายในเวลาที่กำหนด ไม่แฮงค์
6. Auth rotation/reuse-detection (จาก ticket 01)

## Acceptance

- `bun run test` เขียวใน CI (ไม่มี docker)

## Comments

## Comments

> _This was generated by AI during triage._

**resolved (2026-09-20):** รวม 68 tests (shared 5 + server 63) — ssrf suite 27 (allowlist glob, blocked IP 13 กรณี incl. metadata/mapped, redirect limit, redirect นอก allowlist, scheme), StreamService 8 (prebuffer, 206 passthrough, cache, content-type guard, allowlist guard, NOT_FOUND, local, resolver-down), stream route 7 (401/400/429/206/StreamError mapping/concurrent), auth contract 3 + AuthService 10 (rotation/reuse-detection/jwt). ทั้งหมด hermetic — CI ไม่ต้องมี docker
