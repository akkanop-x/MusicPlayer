# 07 — Smoke E2E จริง (เล่นเสียงได้)

Status: resolved

> _This was generated by AI during triage._

## Work

1. ลงทะเบียนผู้ใช้จริงผ่าน API → login → ได้ refresh cookie
2. search → เลือก track → `GET /api/v1/stream/:id` (Range: bytes=0-) → 206 + audio bytes จริง
3. เล่นใน browser ผ่าน audio element ชั่วคราว (dev hook บน placeholder page หรือทดสอบผ่าน browser-use) → **ได้ยินเสียง**
4. seek: `Range: bytes=<กลางเพลง>-` → 206 ที่ offset ถูกต้อง
5. `docker compose stop resolver` → stream → 503 ชัดเจน ภายใน timeout ไม่แฮงค์
6. genre: ถ้ามี credentials → เช็ค tracks.genres ของ track ใหม่

## Acceptance

- บันทึกผลจริงทั้งหมดใน ticket + spec Evidence

## Comments

## Comments

> _This was generated by AI during triage._

**resolved (2026-09-20) — ผลจริง:**

1. register ผ่าน API จริง (migration 0001 run อัตโนมัติ) → cookie
2. `GET /stream/:id` Range bytes=0- → **HTTP 206, audio/webm, 3,433,755 bytes** (`file` ยืนยัน WebM/Opus)
3. **เล่นจริงใน browser** (localhost:8080 same-origin ผ่าน nginx): login → `<audio src="/api/v1/stream/...">` → loadedmetadata (duration 213 s) → canplay → **playing, currentTime 0.67 s วิ่งจริง** ✓✓
4. seek: `currentTime = 60` → เล่นต่อที่ 60.55 s → pause ✓ (และ curl: 206 + `content-range: bytes 1000000-3433754/3433755`)
5. `docker compose stop resolver` → track ที่ cache หมด → **503 UPSTREAM_UNAVAILABLE ใน ~6 s** ไม่แฮงค์; start กลับ → 206 ทันที ✓
6. genre: โค้ดพร้อมแต่ Spotify คืน 403 (ต้องมี Premium — ดู ticket 05)

- **แก้ด้วย:** nginx.conf เดิม `proxy_pass http://server:3001/` ตัด path ผิด → เอา slash ออก + proxy_buffering off (สำคัญกับ stream)
