# 03 — Smoke E2E + DoD + ปิด phase

Status: resolved

> _This was generated by AI during triage._

## งาน

1. Browser จริง (browser-use): login → พิมพ์ค้น (debounce ทำงาน) → เจอผล YouTube/YTMusic
   → เล่นได้จริง → pagination → /track/:id → add-to-queue
2. วัด P95: curl /api/v1/search 10 ครั้ง (ล็อกอินแล้ว) → P95 < 2 s (บันทึกตัวเลขจริง)
3. Validation เต็ม: bun test ทุก package + lint + format + typecheck
4. Tracker resolved + Evidence + Lessons + commit/push → CI เขียว

## DoD (roadmap)

- ค้นหาเจอจาก YouTube/YTMusic และเล่นได้จริง ✓ (browser + เสียงจริง)
- P95 < 2 s ✓ (ตัวเลขวัดจริงลง Evidence)

## Evidence (2026-09-20)

- **P95 search = 0.68 s** (10 ครั้ง q="rick astley" ผ่าน server จริง: min 0.447 /
  median 0.509 / max 0.740 s) — ต่ำกว่า DoD 2 s มาก
- browser จริง: search debounce + 20 ผล, load-more 20→24, /track/:id, play เสียงจริง
  (audio.currentTime 17 s, buffered 191 s, duration 291.8 s), add-to-queue สำเร็จ
- ytm source จริง: q="daft punk"&source=ytm → 3 ผล available=["library","ytm"]
- validation เต็มเขียว: vitest 173 (shared 35 + server 124 + web 14) + eslint +
  prettier + tsc ทุก workspace
- ข้อควรจำ: บทเรียน smoke ครั้งแรกวัด P95 ได้ ~5 ms เพราะ curl ไม่แนบ Bearer → ได้ 401
  เร็ว ๆ — ต้องตรวจ status code คู่กับเวลาเสมอ

## Lessons learned

- /search ตอนนี้ requireAuth แล้ว — ทุกเครื่องมือวัด/ทดสอบต้องแนบ Bearer ก่อนวัดผล
- Lavalink /loadtracks คืนชุดเดียว (~20-30 ผล) — pagination ลึกทำไม่ได้จนกว่าจะใช้
  YouTube Data API (บันทึกใน spec §การตัดสินใจ ข้อ 4 แล้ว)
