# 02 — Search frontend (debounce + pagination + TrackPage)

Status: resolved

> _This was generated by AI during triage._

## งาน

1. `hooks/useSearch.ts` — useDebouncedValue(query, 300) + useQuery key ["search", q, offset]
   staleTime 60s placeholderData keep previous (frontend.md §3.5)
2. `api/index.ts` — searchApi.search(q, {limit, offset}); tracksApi.getById/getBatch
3. `HomePage.tsx` — ค้นขณะพิมพ์ (form submit ยังทำงาน = search ทันที), badge degraded,
   ปุ่ม "โหลดเพลงเพิ่มเติม" เมื่อได้ครบ limit หน้าละ 20 (append ผล), แถวผลลัพธ์ลิงก์เข้า
   /track/:id (ปุ่ม play/add-to-queue คงเดิม)
4. `TrackPage.tsx` (ใหม่ /track/:id) — artwork/artist/album/duration/source + play +
   add-to-queue; PlayerBar/QueuePanel คงอยู่ (layout-level ไม่ unmount — frontend.md §5)
5. `App.tsx` — route /track/:id

## DoD

- web tests: debounce hook (fake timers — พิมพ์รัวยิงครั้งเดียวหลัง 300 ms) + TrackPage
  render/play call — เขียวภายใต้ jsdom
- ทุกคำสั่ง sync queue store หลัง server ตอบ (pattern useQueueCommand เดิม)

## Evidence (2026-09-20)

- web tests 14 ผ่าน (useSearch 6 + TrackPage 4 + audioEngine 4)
- browser จริง: พิมพ์ "rick astley" → debounce ทำงาน (0 ผลก่อน 300 ms) → 20 ผล;
  "โหลดเพลงเพิ่มเติม" → 20→24 append และปุ่มหายเมื่อหน้าไม่เต็ม; /track/:id แสดง
  title/artist/4:52·youtube; กด play → เสียงจริง t=17s buffered 191s; add-to-queue
  → คิว (1) + toast "เพิ่มเข้าคิวแล้ว"

## Lessons learned

- อย่า combine setState-in-effect กับ pagination — ใช้ useInfiniteQuery ตรง ๆ ได้ทั้ง
  dedupe และ getNextPageParam และผ่าน lint rule react-hooks/set-state-in-effect
- fake timers + TanStack Query: ต้องใช้ vi.advanceTimersByTimeAsync/runAllTimersAsync
  (flush microtask คู่กับ timers) ไม่งั้น query ไม่ resolve ภายใน act
- ค่า initial ของ useDebouncedValue ผ่านทันที (useState(value)) — เทสต้องเริ่ม q=""
