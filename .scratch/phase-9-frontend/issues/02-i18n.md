# 02 — web i18n (react-i18next)

Status: resolved

## งาน

- deps: `i18next`, `react-i18next`
- `src/i18n/index.ts` — init จาก resources static (th/en), fallback `'th'`
- `src/i18n/locales/th.json` + `en.json` — key ทุก string ใน UI ที่มีอยู่และใหม่
  (auth/home/search/library/playlist/settings/eq/player/queue/common)
- boot: หลัง login/hydrate → GET /settings → `i18n.changeLanguage(settings.locale)`
- SettingsPage: ปุ่มสลับ th/en → PATCH /settings {locale} → changeLanguage + invalidate
- เปลี่ยนทุก hardcoded string ใน pages/components/audioEngine toasts เป็น `t()`
  (ชื่อเพลง/ศิลปินไม่แปล)

## Test

- i18n init + changeLanguage เปลี่ยน label; boot wiring จาก mock settings;
  switcher เรียก PATCH + changeLanguage

## Evidence (2026-09-21)

- i18n/index.ts (namespace ต่อ section — แก้บั๊กแรกที่ใช้ default ns "translation" แล้ว
  key "common:logout" หาไม่เจอ) + th/en.json ครบทุก key (test ตรวจคู่), applyLocaleFromServer
- SettingsPage switcher (th/en → PATCH + changeLanguage + optimistic) + t() ครบทุก
  pages/components/toasts (audioEngine/handlers ใช้ i18next.t ตรง)
- i18n.test 4 cases + ชุด component tests อื่นยืนยัน label — web รวม 59 เขียว
