# 05 — Smoke test กับ Lavalink container จริง

Status: resolved

> _This was generated by AI during triage._

## Context

roadmap Phase 2: "`GET /v4/info` ผ่าน + `ytsearch:` และ `spsearch:` คืนผลได้" — จุดพิสูจน์ว่า config/plugin จริงเวิร์ค (ความจริงเจอกับเอกสาร)

## Work

1. `apps/server/scripts/smoke-lavalink.ts` — รันด้วย tsx, อ่าน `LAVALINK_URL` (default http://localhost:2333) + `LAVALINK_PASSWORD` (default youshallnotpass):
   - step 1: GET /v4/info → พิมพ์ version + plugins + sourceManagers
   - step 2: loadtracks `ytsearch:<คำถาม>` → พิมพ์ loadType + title แรก
   - step 3: loadtracks `spsearch:<คำถาม>` → เหมือนกัน (ถ้าโดน error เพราะไม่มี credentials → พิมพ์เตือนชัดว่าต้องตั้ง SPOTIFY_CLIENT_ID/SECRET แล้ว fail step นี้)
   - exit non-zero เมื่อ step จำเป็นล้มเหลว
2. รันจริง: `docker compose up -d lavalink` → รอ boot → รัน smoke → บันทึกผลจริง
3. ทดสอบ fail-soft จริง: `docker compose stop lavalink` → `curl /api/v1/search` → ต้องได้ 503 ภายใน ~10 s

## Acceptance

- ผลจริง (version/plugin/ผลค้นหา) ถูกบันทึกใน ticket นี้ + spec Evidence
- หมายเหตุ: `spsearch:` ผ่านเฉพาะเมื่อมี Spotify credentials ใน .env — ถ้าผู้ใช้ยังไม่มี ให้ยืนยันว่า step อื่นผ่านและระบุเป็น pending item ของผู้ใช้

## Comments

## Comments

> _This was generated by AI during triage._

**resolved (2026-09-20):** smoke script `apps/server/scripts/smoke-lavalink.ts` — ผลจริง:

- `/v4/info` → version 4.2.2, plugins youtube-plugin@1.18.2 + lavasrc-plugin@4.8.3 ✅
- `ytsearch:"โลกโบราณ"` → loadType=search, 20 tracks ✅
- `spsearch:` → loadType=error ⚠️ **pending ผู้ใช้**: ยังไม่มี SPOTIFY_CLIENT_ID/SECRET ใน .env (สมัครที่ developer.spotify.com → ใส่ใน .env → `docker compose up -d lavalink`)
- **fail-soft จริง:** `docker compose stop lavalink` → `GET /api/v1/search` → HTTP 503 `UPSTREAM_UNAVAILABLE` + `degraded:["yt"]` ใน ~2.9 s ✅
- **recovery:** start ใหม่ → breaker half-open → 200 กลับมา ✅
- **upsert/dedupe จริง:** search ซ้ำ → rows ตรงกับ distinct (source_name, source_identifier) ✅
