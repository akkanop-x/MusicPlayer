# 03 — SearchService + tracks.repo.upsert + GET /api/v1/search

Status: resolved

> _This was generated by AI during triage._

## Context

api.md §3 #6: `GET /search?q=&limit=20&offset=0` → `{ tracks: TrackDTO[], sources: {available, degraded[]} }`; backend.md SearchService (normalize + upsert + dedupe); database.md `tracks` UNIQUE(source_name, source_identifier). **`stream_url`/lavalink_encoded ห้ามออกไปใน DTO** (database.md — TrackDTO กำหนดใน api.md §1)

## Work

1. `packages/shared/src/types.ts` — `TrackDTO { id, title, artist, album, durationMs, isStream, isSeekable, artworkUrl, sourceName, isLiked }` + `SearchResponseDTO { tracks, sources: {available: string[], degraded: string[]} }` (export ผ่าน index)
2. `repositories/tracks.repo.ts` — `upsertTrack(db, t)`: insert … onConflictDoUpdate target `[sourceName, sourceIdentifier]` set metadata ใหม่ + `resolvedAt/updatedAt = now()`, returning; `TrackUpsert` type (sourceName, sourceIdentifier, title, artist, album?, durationMs (isStream→0), isStream, isSeekable, artworkUrl?, isrc?, lavalinkEncoded?)
3. `services/SearchService.ts` — `createSearchService({ db, lavalink })`:
   - `search(q, { source = "yt", limit = 20 })`; mapping `yt→ytsearch:`, `ytm→ytmsearch:`, `sp→spsearch:`
   - เรียก `loadTracks(prefix + q)` → flatten ตาม loadType: track→[1], search→array, playlist→`data.tracks`, empty→[]
   - slice(limit) → map เป็น TrackUpsert (album จาก `pluginInfo.albumName` ของ LavaSrc ถ้ามี) → upsert ทุกเพลง → map row → TrackDTO (isLiked: false — Phase 2)
   - loadType=error / LavalinkError ใด ๆ → โยนต่อ (route จัดการ)
4. `routes/search.routes.ts` — GET `/api/v1/search`; validate query ด้วย zod: `q` (trim, 1–200), `limit` int 1–50 default 20, `source` enum default `yt`; fail → 400 VALIDATION_ERROR; LavalinkError/UnavailableError → 503 UPSTREAM_UNAVAILABLE (`sources: {available: [], degraded: [source]}` ใน details) — ตาม DoD fail-soft
5. Wire ใน `app.ts`: `buildApp(env, deps?: { db?, lavalink?, search? })` — ยัง inject ได้สำหรับ test; `index.ts` ส่ง db จริง + LavalinkClient จาก env; route comment: ยังไม่มี auth guard (ระบบ auth เป็น phase ถัด ๆ ไป)

## Acceptance

- Contract tests (ticket 05) ครอบ behavior ทั้งหมดของ service+route
- lint/typecheck ผ่าน

## Comments

## Comments

> _This was generated by AI during triage._

**resolved (2026-09-20):** `packages/shared/src/types.ts` (TrackDTO + SearchResponseDTO); `tracks.repo.ts` upsert ด้วย onConflictDoUpdate target (sourceName, sourceIdentifier); `SearchService` flatten 5 loadTypes + album จาก `pluginInfo.albumName` ของ LavaSrc; `GET /api/v1/search` validate ด้วย zod (q 1–200, limit 1–50, source yt|ytm|sp) และ map LavalinkError → 503 UPSTREAM_UNAVAILABLE. `buildApp` รับ deps inject ได้ (db/lavalink/search) — route ยังไม่มี auth guard ตามที่ spec กำหนด. ทดสอบจริง: `curl /api/v1/search?q=one+more+time` → 200 TrackDTO จาก youtube จริง.
