# 04 — Contract tests ครบ 5 loadTypes + fail-soft

Status: resolved

> _This was generated by AI during triage._

## Context

roadmap Phase 2: "Contract tests กับ mock Lavalink (fixture ทั้ง 5 loadType)" — ต้อง hermetic (รันใน CI โดยไม่มี docker)

## Work

`apps/server/src/routes/search.contract.test.ts` (หรือ test ใน services) — สร้าง app ผ่าน `buildApp` โดย inject fake LavalinkClient (แค่ `loadTracks`) + fake upsert (จับ upserts, คืน `{id: crypto.randomUUID()}`):

1. **loadType=track** → 200 `{tracks:[1 ตัว]}` + upsert 1 ครั้ง + DTO ถูก field (ไม่มี stream_url/lavalinkEncoded หลุดออก)
2. **loadType=search** → 200 หลายเพลง + upsert ครบ
3. **loadType=playlist** → 200 flatten จาก `data.tracks`
4. **loadType=empty** → 200 `{tracks: []}` + ไม่มี upsert
5. **loadType=error** → 503 `UPSTREAM_UNAVAILABLE`
6. LavalinkUnavailableError (ล่ม/timeout/breaker) → 503 + degraded มี source ที่ขอ
7. ไม่มี `q` / `q` ว่างเปล่า → 400 VALIDATION_ERROR; limit/source ผิด → 400
8. source mapping: `?source=ytm` → identifier เริ่ม `ytmsearch:`, `?source=sp` → `spsearch:`
9. limit: ผลจาก Lavalink 12 → `?limit=5` ได้ 5 และ upsert 5

Fixture ต้องเลียน shape v4 จริง (encoded base64, info ครบ field ตาม lavalink.md §6.1, spotify track มี `pluginInfo.albumName`)

## Acceptance

- `pnpm --filter @musicplayer/server test` เขียวใน CI (ไม่ต้องมี postgres/docker)

## Comments

## Comments

> _This was generated by AI during triage._

**resolved (2026-09-20):** `search.contract.test.ts` ครอบครบ: 5 loadTypes (track/search/playlist/empty/error), fail-soft UnavailableError → 503, source mapping (ytmsearch:/spsearch:), limit ตัดผล+upsert, validation 6 กรณี → 400, ไม่มี streamUrl/lavalinkEncoded หลุดใน response. รวมทุก test ของ server = **24/24 เขียว** (hermetic — CI ไม่ต้องมี docker).
